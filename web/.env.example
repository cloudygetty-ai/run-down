# Optional: override the default map tile style
# Default uses CartoDB Dark Matter (free, no token required)
VITE_MAP_STYLE=https://basemaps.cartocdn.com/gl/dark-matter-gl-style/style.json

# Optional: Socket.io backend URL (falls back to mock data if not set)
VITE_SOCKET_URL=
