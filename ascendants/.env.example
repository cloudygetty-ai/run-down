# WebSocket relay the client connects to for online play.
# The relay only forwards inputs between two peers; it never simulates a match.
VITE_RELAY_URL=ws://localhost:8787

# Port the relay binds to (server/index.ts).
PORT=8787
