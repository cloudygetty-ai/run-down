name: CI

on:
  push:
    branches: ["main", "dev", "claude/**"]
  pull_request:
    branches: ["main", "dev"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Type Check · Lint · Test
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run type-check

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test -- --ci --coverage --coverageReporters=text-summary --reporters=default

      - name: Upload coverage
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/
          retention-days: 7

  gridlock:
    name: GRIDLOCK Simulation Tests
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # WHY node 20 and no install step: the GRIDLOCK simulation core is
      # dependency-free ES modules on the built-in node test runner, so it needs
      # neither the RN toolchain nor npm ci.
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Test simulation core
        working-directory: games/gridlock
        run: node --test

  android-build:
    name: Android Build Check
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: quality
    # WHY: android/ is not committed until the native shell is bootstrapped via
    # react-native init. Skip this job rather than fail every PR in the meantime.
    if: >
      (github.ref == 'refs/heads/main' || github.event_name == 'pull_request') &&
      hashFiles('android/gradlew') != ''

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install dependencies
        run: npm ci

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('android/**/*.gradle*', 'android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: gradle-

      - name: Build Android (debug)
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon

